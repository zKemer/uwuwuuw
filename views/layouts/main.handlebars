<!doctype html>
<html lang="tr">

    <style>

.btn1 {
background-color:#99a3ec;
border:1px solid #99a3ec;
display:inline-block;
cursor:pointer;
color:#ffffff;
font-family:Arial;
font-size:19px;
padding:20px 63px;
text-decoration:none;
text-shadow:0px 1px 0px #2f6627;
}
.btn1:hover {
background-color:#99a3ec;
}
.btn1:active {
position:relative;
top:1px;
}
 
      
.adresler{
  position: relative;
  left: 1100px;
  top: 5px;
}

    .username{
  position: relative;
  left: 50px;
  top: 22px;
}
      .avatar{
  position: relative;
  left: 3px;
  top: -7px;
}
      
</style>
    
    
        {{#if user}}
                
        
           <div class="username"> <font size="3" face="Tahoma"  color="#99a3ec" >{{user.username}}</font> </div>     <div class="avatar"> <img style="border-radius:50%" width="40" height="40" src="https://cdn.discordapp.com/avatars/{{user.id}}/{{user.avatar}}.png"></div> 
  
                   </a>
           
           
        {{else}}
            
            <div class="adresler"> <a class="btn1" href="/giris" style="text-decoration:none"><i class="fas fa-sign-in-alt "></i> Giriş Yap</a></div>
        {{/if}}

    
    
    {{{body}}}
    
    

              <script src="https://cdn.jsdelivr.net/npm/darkmode-js@1.5.7/lib/darkmode-js.min.js"></script>

  <script>
  const options = {
  bottom: '64px', // default: '32px'
  right: 'unset', // default: '32px'
  left: '32px', // default: 'unset'
  time: '0.5s', // default: '0.3s'
  mixColor: '#f0f0f0', // default: '#fff'
  backgroundColor: '#f8f8f8',  // default: '#fff'
  buttonColorDark: '#080808',  // default: '#100f2c'
  buttonColorLight: '#f0ffff', // default: '#fff'
  saveInCookies: false, // default: true,
  label: '🌓', // default: ''
  autoMatchOsTheme: true // default: true
}
 
const darkmode = new Darkmode(options);
darkmode.showWidget();
  </script>
  
  </body>
</html>